spring:
  datasource:
    url: jdbc:h2:mem:jobsdb
    driver-class-name: org.h2.Driver
    username: postgres
    password: 12345678

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true

server:
  port: 8080